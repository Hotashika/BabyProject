package com.example.bebegim.ui.theme

import androidx.compose.ui.graphics.Color

// === BABY GUARD ANA RENK PALETİ ===

// Primary Colors - Güvenlik ve Güven Teması
val SafeBlue = Color(0xFF1976D2)          // Ana güvenlik mavisi
val LightSafeBlue = Color(0xFF64B5F6)     // Dark mode için açık mavi
val DarkBlue = Color(0xFF0D47A1)          // Koyu mavi vurgu
val LightBlue = Color(0xFFE3F2FD)         // Açık mavi container
val DarkBlueContainer = Color(0xFF1565C0) // Dark mode container

// Secondary Colors - Sıcaklık ve Dikkat
val WarmOrange = Color(0xFFFF9800)        // Sıcak turuncu
val SoftWarmOrange = Color(0xFFFFB74D)    // Yumuşak turuncu (dark mode)
val DarkOrange = Color(0xFFE65100)        // Koyu turuncu
val LightOrange = Color(0xFFFFF3E0)       // Açık turuncu container
val DarkOrangeContainer = Color(0xFFEF6C00) // Dark mode container

// Tertiary Colors - Sakinlik ve Denge
val SoftMint = Color(0xFF4CAF50)          // Yumuşak mint yeşili
val DarkSoftMint = Color(0xFF66BB6A)      // Dark mode mint
val DarkMint = Color(0xFF2E7D32)          // Koyu mint
val VeryLightMint = Color(0xFFF1F8E9)     // Çok açık mint container
val DarkMintContainer = Color(0xFF388E3C) // Dark mode container

// Background Colors
val PastelBlueWhite = Color(0xFFF0F6FF)   // Pastel mavi beyaz arka plan
val SoftPastelBlue = Color(0xFFF5F9FF)    // Yumuşak pastel mavi yüzey
val DarkPastelBlue = Color(0xFF1A2332)    // Koyu pastel mavi arka plan
val DarkPastelBlueSurface = Color(0xFF243142) // Koyu pastel mavi yüzey

// Text Colors
val DeepGray = Color(0xFF212121)          // Koyu gri metin
val MediumGray = Color(0xFF757575)        // Orta gri
val LightGray = Color(0xFFBDBDBD)         // Açık gri
val VeryLightGray = Color(0xFFF5F5F5)     // Çok açık gri
val DarkGray = Color(0xFF424242)          // Koyu gri
val DarkGraySurface = Color(0xFF2C2C2C)   // Dark mode gri yüzey

// === ÖZEL DURUM RENKLERİ ===

// Acil Durum ve Hata Renkleri
val AlertRed = Color(0xFFD32F2F)          // Ana alarm kırmızısı
val LightAlertRed = Color(0xFFEF5350)     // Dark mode için açık kırmızı
val DarkRed = Color(0xFFB71C1C)           // Koyu kırmızı
val LightRed = Color(0xFFFFEBEE)          // Açık kırmızı container
val DarkRedContainer = Color(0xFFC62828)  // Dark mode kırmızı container

// Başarı ve Güvenlik Durumu
val SuccessGreen = Color(0xFF388E3C)      // Güvenlik yeşili
val LightSuccessGreen = Color(0xFF66BB6A) // Açık yeşil
val DarkSuccessGreen = Color(0xFF1B5E20)  // Koyu yeşil
val LightGreenContainer = Color(0xFFF1F8E9) // Açık yeşil container

// Uyarı Renkleri
val WarningAmber = Color(0xFFF57C00)      // Uyarı turuncu-sarısı
val LightWarningAmber = Color(0xFFFFB74D) // Açık uyarı rengi
val DarkWarningAmber = Color(0xFFE65100)  // Koyu uyarı rengi
val LightAmberContainer = Color(0xFFFFF8E1) // Açık amber container

// Bilgi Renkleri
val InfoBlue = Color(0xFF0288D1)          // Bilgi mavisi
val LightInfoBlue = Color(0xFF4FC3F7)     // Açık bilgi mavisi
val LightInfoContainer = Color(0xFFE1F5FE) // Açık bilgi container

// === ÖZEL BABY GUARD RENKLERİ ===

// Bebek İzleme Durumları
val BabySleeping = Color(0xFF7986CB)      // Bebek uyuyor - mor tonları
val BabyAwake = Color(0xFF81C784)         // Bebek uyanık - yeşil
val BabyCrying = Color(0xFFFFB74D)        // Bebek ağlıyor - turuncu
val BabyDanger = Color(0xFFE57373)        // Tehlike durumu - kırmızı

// Çevre Durumu Renkleri
val TemperatureNormal = Color(0xFF4CAF50)  // Normal sıcaklık
val TemperatureHot = Color(0xFFFF9800)     // Sıcak
val TemperatureCold = Color(0xFF2196F3)    // Soğuk
val HumidityGood = Color(0xFF00BCD4)       // İyi nem oranı

// Gece Modu Özel Renkleri
val NightModeBlue = Color(0xFF1A237E)      // Gece modu ana rengi
val NightModeAccent = Color(0xFF3F51B5)    // Gece modu vurgu rengi

// Gradient Renkleri (Opsiyonel)
val GradientStart = Color(0xFF1976D2)      // Gradient başlangıç
val GradientEnd = Color(0xFF1565C0)        // Gradient bitiş